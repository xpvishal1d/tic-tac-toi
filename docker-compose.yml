services:
  postgres:
    image: postgres:16-alpine
    container_name: tic-tac-toi-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: tictactoi
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
